#!/bin/bash

# opencode-sync TUI - Simple bash-based interface
# This script runs the reliable bash TUI

SYNC_DIR="$HOME/.local/share/opencode-sync"
BASH_TUI_WRAPPER="$SYNC_DIR/run-tui.sh"

# Run the bash TUI wrapper
exec bash "$BASH_TUI_WRAPPER" "$@"